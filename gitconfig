[user]
    name = David Morris
    email = david.morris@arachnys.com
[alias]
    d = diff --color
    dc = diff --cached --color
    ds = diff --stat
    s = status
    st = status
    ci = commit
    co = checkout
    cim = commit -m
    cam = commit -a -m
    last = log -n1 --color
    l1 = log -n1 --color
    lp1 = log -n1 --color -p
    l = log --color
    lp = log -p --color
    lq = log --graph --pretty=format:'%C(auto)%h %s'
    lqs = log --graph --pretty=format:'%C(auto)%h %s' -n 10
    lgo = log --graph --oneline
    lgod = log --graph --oneline --decorate
    lo = log --oneline
    lol = log --oneline --color -n20
    lod = log --oneline --decorate
    lg = log --graph
    lgd = log --graph --decorate
    lr = log -n10 --oneline
    lrg = log -n10 --graph --oneline
    los = log --oneline --stat
    lrs = log -n5 --oneline --stat
    lot = log --date=iso-local --pretty=format:'%C(auto)%h %ai %s'
    a = add
    aa = add -A
    ap = add -p
    au = add -u
    r = rebase
    submod = submodule
    smod = submodule
    sm = submodule
    b = branch
[push]
	default = simple
